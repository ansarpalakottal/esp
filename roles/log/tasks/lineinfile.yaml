---
###WRITING LOG TO THE LOG FILE
- name: writing restart log to the log file
  lineinfile:
    line: "LOG FOR RESTARTING TOMCAT ::::::::::::
                {{ output.state }}"

    dest: "{{ dir }}/esp_deploy_{{ dt }}_{{ tm }}.log"
    insertafter: EOF

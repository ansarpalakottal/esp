---
- name: writing stop log to log file
  lineinfile:
    line: "LOG FOR RESTARTING TOMCAT ::::::::::::
                {{ output.state }}"

    dest: "{{ dir }}/esp_deploy_{{ dt }}_{{ tm }}.log"
    insertafter: EOF
